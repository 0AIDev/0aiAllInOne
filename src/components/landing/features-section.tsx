"use client";import {  BarChart3,  Building2,  Shield,  ShieldCheck,  Shrink,  Zap,} from "lucide-react";import { cn } from "@/lib/utils/cn";import { useScrollReveal } from "@/hooks/use-scroll-reveal";import { useLocale } from "@/i18n/locale-provider";const features = [  {    number: "01",    icon: Shield,    title: "features.card1Title",    description: "features.card1Desc",  },  {    number: "02",    icon: Zap,    title: "features.card2Title",    description: "features.card2Desc",    dark: true,  },  {    number: "03",    icon: Shrink,    title: "features.card3Title",    description: "features.card3Desc",  },  {    number: "04",    icon: Building2,    title: "features.card4Title",    description: "features.card4Desc",  },  {    number: "05",    icon: ShieldCheck,    title: "features.card5Title",    description: "features.card5Desc",  },  {    number: "06",    icon: BarChart3,    title: "features.card6Title",    description: "features.card6Desc",  },];function FeatureCard({  number,  icon: Icon,  title,  description,  dark,  index,}: (typeof features)[number] & { index: number }) {  const { t } = useLocale();  const { ref, isVisible } = useScrollReveal();  return (    <div      ref={ref}                className={cn(        "group rounded-[14px] border border-[rgba(15,15,14,0.08)] p-6 transition-all duration-500 sm:p-8",        dark          ? "border-transparent bg-[#0F0F0E] text-white"          : "bg-white hover:-translate-y-0.5 hover:shadow-[0_4px_20px_rgba(15,15,14,0.06)]",        isVisible ? "translate-y-0 opacity-100" : "translate-y-8 opacity-0"      )}      style={{ transitionDelay: `${index * 80}ms` }}    >      <div className="flex items-start justify-between">        <div                            className={cn(            "flex h-10 w-10 items-center justify-center rounded-[10px]",            dark ? "bg-white/10" : "bg-[rgba(15,15,14,0.04)]"          )}        >          <Icon className={cn("h-5 w-5", dark ? "text-white" : "text-[#0F0F0E]")} />        </div>        <span          className={cn(            "text-xs font-medium",            dark ? "text-white/40" : "text-[#7A7870]"          )}        >          {number}        </span>      </div>      <h3        className={cn(          "mt-5 text-lg font-medium tracking-[-0.01em]",          dark ? "text-white" : "text-[#0F0F0E]"        )}        style={{ fontFamily: "'Inter Tight', sans-serif" }}      >        {t(title)}      </h3>      <p        className={cn(          "mt-2 text-sm leading-relaxed",          dark ? "text-white/60" : "text-[#3A3A37]"        )}      >        {t(description)}      </p>    </div>  );}export function FeaturesSection() {  const { t } = useLocale();  const { ref: tagRef, isVisible: tagVisible } = useScrollReveal();  const { ref: titleRef, isVisible: titleVisible } = useScrollReveal();  const { ref: descRef, isVisible: descVisible } = useScrollReveal();  const { ref: gridRef, isVisible: gridVisible } = useScrollReveal({    threshold: 0.05,  });  return (    <section className="py-24 sm:py-32">      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">        {/* Tag */}        <p          ref={tagRef}          className={cn(            "text-center text-xs font-medium uppercase tracking-[0.15em] text-[#7A7870] transition-all duration-700",            tagVisible ? "translate-y-0 opacity-100" : "translate-y-4 opacity-0"          )}        >          {t("features.tag")}        </p>        {/* Title */}        <h2          ref={titleRef}          className={cn(            "mt-5 text-center text-[clamp(32px,5vw,48px)] font-medium leading-[1.15] tracking-[-0.02em] text-[#0F0F0E] transition-all duration-700 delay-100",            titleVisible ? "translate-y-0 opacity-100" : "translate-y-6 opacity-0"          )}          style={{ fontFamily: "'Inter Tight', sans-serif" }}        >          {t("features.title")}{" "}          <em            className="italic"            style={{ fontFamily: "'Instrument Serif', serif", fontWeight: 400 }}          >            {t("features.titleEmphasis")}          </em>{" "}          built in.        </h2>        {/* Description */}        <p          ref={descRef}          className={cn(            "mx-auto mt-4 max-w-xl text-center text-[18px] leading-relaxed text-[#3A3A37] transition-all duration-700 delay-200",            descVisible ? "translate-y-0 opacity-100" : "translate-y-6 opacity-0"          )}        >          {t("features.subtitle")}        </p>        {/* Grid */}        <div          ref={gridRef}          className={cn(            "mt-14 grid gap-4 transition-all duration-700 delay-300 sm:grid-cols-2 lg:grid-cols-3",            gridVisible ? "translate-y-0 opacity-100" : "translate-y-8 opacity-0"          )}        >          {features.map((feature, idx) => (            <FeatureCard key={feature.number} {...feature} index={idx} />          ))}        </div>      </div>    </section>  );}
